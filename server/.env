# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password123
POSTGRES_DATABASE=streamify

# Connection strings (use template literals with backticks)
DATABASE_URL=`postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5433/${POSTGRES_DATABASE}?schema=public`
POSTGRES_URI=`postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5433/${POSTGRES_DATABASE}`

# App config
NODE_ENV=development
APPLICATION_PORT=4000
APPLICATION_URL=`http://localhost:${APPLICATION_PORT}`

# Security
COOKIES_SECRET=secret
SESSION_SECRET=secret
SESSION_NAME=session
SESSION_DOMAIN=localhost
SESSION_MAX_AGE=30d
SESSION_HTTP_ONLY=true
SESSION_SECURE=false
SESSION_FOLDER=sessions:

# Redis
REDIS_USER=default
REDIS_PASSWORD=123456
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_TTL=86400
REDIS_URI=`redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}`

# CORS
ALLOWED_ORIGIN=http://localhost:3000
GRAPHQL_PREFIX=/graphql